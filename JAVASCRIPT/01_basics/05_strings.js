const name = 'prathamesh'
const age = 22
const sname = "Jadhav"

// console.log(name+" " + age + " "+ sname);

console.log(`Hello my name is ${name}, my sername is ${sname} and my age is ${age}`);

const newGame = new String('NewGame')
console.log(newGame[0]);
console.log(newGame.__proto__);
